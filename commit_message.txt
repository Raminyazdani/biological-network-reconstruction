Ramin Yazdani | MetaFlux | main | feat(data): Add missing openpyxl and xlrd imports for Excel handling

Fixed missing imports for Excel and ODS file handling libraries.
Added explicit import statements for openpyxl (xlsx), xlrd (xls), and odfpy (ods).

Root cause: Pandas can handle Excel files automatically if libraries are installed,
but best practice requires explicit imports to ensure proper dependency tracking
and avoid runtime errors on fresh environments.

Imports added to notebook's dependency cell:
- import openpyxl  # for .xlsx files
- import xlrd      # for .xls files  
- import odf       # for .ods files

Tested data loading from all four reference datasets successfully.

Verified: All reference datasets load correctly with explicit imports.
